---
// data imports
import * as site from "../data/site.json";

interface Props {
  title?: string;
  description?: string;
  author?: string;
}

const { title, description } = Astro.props;
const { version } = site;
const headerTitle = "MackOS";
---

<div class="card__wrapper">
  <div class="card alt featured" id="about">
    <header class="card__header">
      <img
        src="/assets/images/title-bar-close.png"
        height="28"
        width="28"
        class="control close"
        alt="Close"
      />
      <div class="title">
        <h3 class="card__title">{headerTitle} {version}</h3>
        <span class="icon">&nbsp;</span>
      </div>
      <img
        src="/assets/images/title-bar-expand.png"
        height="28"
        width="28"
        class="control expand"
      />
      <img
        src="/assets/images/title-bar-collapse.png"
        height="28"
        width="28"
        class="control collapse"
      />
    </header>

    <section class="card__content">
      <article>
        <section class="hero-image">
          <img src="/assets/images/mack-photo.jpg" alt="Mack" id="mack" />
          <svg xmlns="http://www.w3.org/2000/svg">
            <filter id="noiseFilter">
              <feTurbulence
                type="fractalNoise"
                baseFrequency="0.5"
                numOctaves="3"
                stitchTiles="stitch"></feTurbulence>
            </filter>

            <rect width="100%" height="100%" filter="url(#noiseFilter)"></rect>
          </svg>
        </section>
      </article>
      <a href="#popup1" class="button primary">About Mack</a>
    </section>
  </div>
</div><!-- end: .card-wrapper -->

<style>
  .control {
    opacity: 0;
  }
  .card__content {
    padding: 0;
    position: relative;
  }
  .card__content article {
    display: flow-root;
    padding: 0;
    position: relative;
    width: 100%;
    max-width: 100%;
    z-index: 1;
  }
  .card__content .button {
    position: absolute;
    right: 1rem;
    bottom: 1rem;
    z-index: 2;
  }
  .card__content .hero-image {
    margin: 0 !important;
    height: 100%;
    width: 100%;
  }
  .card__content .hero-image img#mack {
    border: none;
    box-shadow: none;
    display: block;
    height: auto;
    margin: 0;
    min-height: 300px;
    object-fit: cover;
    shape-outside: url("/assets/images/mack.png");
    shape-margin: 1rem;
    width: 100%;
  }
</style>
